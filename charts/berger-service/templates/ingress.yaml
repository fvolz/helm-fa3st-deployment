{{- if .Values.berger-service.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "faaast-service.fullname" . }}
  labels:
    {{- include "faaast-service.labels" . | nindent 4 }}
  {{- with .Values.berger-service.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.berger-service.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.berger-service.ingress.tls }}
  tls:
    {{- range .Values.berger-service.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.berger-service.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "faaast-service.fullname" $ }}
                port:
                  number: {{ $.Values.berger-service.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
