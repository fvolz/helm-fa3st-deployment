{{- if .Values.fa3st.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "faaast-service.fullname" . }}
  labels:
    {{- include "faaast-service.labels" . | nindent 4 }}
  {{- with .Values.fa3st.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.fa3st.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.fa3st.ingress.tls }}
  tls:
    {{- range .Values.fa3st.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.fa3st.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "faaast-service.fullname" $ }}
                port:
                  number: {{ $.Values.fa3st.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
