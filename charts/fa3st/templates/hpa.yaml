{{- if .Values.fa3st.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "faaast-service.fullname" . }}
  labels:
    {{- include "faaast-service.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "faaast-service.fullname" . }}
  minReplicas: {{ .Values.fa3st.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.fa3st.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.fa3st.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.fa3st.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.fa3st.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.fa3st.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
